@using Microsoft.AspNetCore.Identity;
@*@using Erp_TEST.Service.LayoutData;
@inject ILayoutDataService layoutDataService;*@


@inject SignInManager<AccountUser> SignInManager
@inject UserManager<AccountUser> UserManager

<ul class="navbar-nav">
   
    @if (SignInManager.IsSignedIn(User))
    {
        @*@{
            var isAuthenticated =  layoutDataService.IsAuthenticated();
            var getRole = layoutDataService.GetUserRole();
        }*@

        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {

        <li id="nav_right">
            <span style=" margin-right: 30px;" href="">
                Hello, Guest!
            </span>
        </li>
        <li>

            <a style=" margin-right: 30px;" asp-area="" asp-controller="Account" asp-action="Register">Register</a>
            <a asp-area="" asp-controller="Account" asp-action="Login" &copy;>Log in</a>
        </li>
    }
</ul>
